package week7Day3;

public class stackAndHeap {

	public static void main(String[] args) {
		/*
		 * Memory in java is divided in 3 parts:
		 * 1: Stack ; which permeative data types and a
		 * Address of references data type are stored in to it.
		 * 
		 * 2) Heap : which the references data type are stored there.
		 * 
		 * 3) Static : which the static variable stored there
		 * in static we can not use keyword"this" because they don't have address in stack
		 * and also we can not have non static variable in static methods.
		 */

		/*
		 * Static or constant pool is in heap and all literal " test " is stored there.
		 * if there is one literal already there , it is going to replace, not creating a 
		 * new object
		 */
		
		/*
		 *  == equality operator: compare what you have in stack
		 *  .equals() method will compare the actual values of an object
		 *  
		 *  for primitive data types we don't have .equal() methods because they don't have 
		 *  methods. 
		 */
		
		/*
		 * you can not use non static variable into the static method
		 * and this keyword with static method or variable.
		 */
		
		// C language is the core programming language but because it is so hard-
		//- other languages like java, paython and ... are made to make it easier.
		
		
		
		
		
		
		
	}

}
