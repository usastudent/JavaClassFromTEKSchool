package week6Day3;

public class SelfNoteFromClass {

	public static void main(String[] args) {
		
		/*
		 * inheritance:
		 * if we have parent class(A) and child class (B)
		 * B extends A = means B is subclass of A
		 * parent class is equal to super class
		 */
		
		/*
		 * job of constructor:
		 * to initialize all the variables of the class
		 * if we want to get to the parent class constructor 
		 * super() key is used like this and it chains classes together -
		 * -all the way to the parent class.
		 * because variables are private so we access them through public construcor 
		 * and super key
		 * 
		 * super () : super key (signature) when we call it we match inside the() the signature
		 */
		
	}

}
